import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_responsive_ui/flutter_responsive_ui.dart';

void main() {
  test('Package loads correctly', () {
    expect(ResponsiveRow, isNotNull);
    expect(ResponsiveColumn, isNotNull);
  });
}
